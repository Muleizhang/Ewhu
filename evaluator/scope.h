// 负责局部变量的作用范围
#include <unordered_map>
#include <string>
#include <memory>
class Scope
{
public:
    Scope(){};
    ~Scope(){};

public:
    // std::unordered_map<std::string, std::shared_ptr<objec>>
}